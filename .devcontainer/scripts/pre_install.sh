#!/bin/bash
#apt-get -yq install man fish
#curl -L https://get.oh-my.fish > install
#fish install --path=~/.local/share/omf --config=~/.config/omf
#apt-get update -yq && apt-get -yq install man fish && curl -L https://get.oh-my.fish > install && fish install --path=~/.local/share/omf --config=~/.config/omf
